import React from 'react';

export const DarkCtx = React.createContext(false);
export const FontCtx = React.createContext({ font: null, scale: 1 });
export const SiteChromeContext = React.createContext(null);
